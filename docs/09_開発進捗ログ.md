# 09. 開発進捗ログ・やることリスト（子ドキュメント）

> 親: [00_要件定義書.md](./00_要件定義書.md)
> 直近の実装セッションで何をやったか（完了リスト）と、次にやる候補（バックログ）をここに残す。
> 最終更新: 2026-07-19

---

## 現在の到達点（サマリ）

| 領域 | 状態 |
|---|---|
| 学習（すごろく `/learn`） | **29章 / 117ノード**（レッスン87・テスト30）。3レベル制（初級6・中級11・上級12）。順序は `chapters` 配列順、a3が最終章 |
| レッスンの中身 | 物語＋`takeaways`＋写経`codeSample`＋ミニ練習`practice`＋図鑑相互リンク`relatedSlugs` |
| テスト | 8割合格で解放。合格後の深掘り`deepDive`＋バッジ獲得（`awards.ts`） |
| コース目次 | `/curriculum`（全章一覧・レベル達成トロフィー・続きから再開） |
| 弱点復習 | `/learn/review`（テスト誤答だけ再挑戦・正解で消える）。記録は `cocre:journey-weak:v1` |
| 図鑑 | **298語すべてにデモ実装＝デモ100%**。詳細⇔レッスンの双方向リンク |
| 課金設計 | 無料/VIP(サブスク)/lifetime(買い切り)。中上級コンテンツがVIPの主軸（docs/05） |
| 同期 | お気に入り・成績・進捗・既読を Supabase 同期（`SupabaseSync`。テーブル未作成でも無害） |

---

## 完了リスト（実装セッション 2026-07-16〜19）

### コンテンツ拡充
- [x] 初心者コースと必修コースの用語重複を解消（roadmap廃止→レッスン末尾の図鑑導線に吸収、`/map`→`/learn`リダイレクト）
- [x] 3レベルのコース制（`level`/`access`）を導入し、中上級をVIPの主軸に
- [x] 中級を b1〜m11 まで拡充（HTML/CSS/JS/配列/データ/API実践/フォーム/状態/localStorage/Tailwind/デバッグ）
- [x] 上級を a1〜a11・a3 まで拡充（React/道具/テスト/速く/a11y/Git実践/セキュリティ/型/デザイン/SEO/AI）
- [x] 全レッスンに写経コード・ミニ練習を付与、テストに深掘りを付与
- [x] 章タイトルを「動詞で伝わる」題名に統一

### 図鑑
- [x] 中上級の新語を図鑑化（variable/function/array/props/state/tailwind/fetch/hash/xss ほか）
- [x] **デモ未実装だった27語すべてに図解デモを実装 → デモ100%達成**
- [x] `array`・`loop` をインタラクティブ化
- [x] 図鑑詳細→レッスンの逆リンク（`lessonForSlug`）

### 機能・UX
- [x] コース目次ページ `/curriculum`（レベル達成トロフィー・続きから再開）
- [x] レッスン弱点復習 `/learn/review`
- [x] `/learn` にレベルジャンプ＋弱点復習バナー、レッスン詳細に現在地表示（例「初級コース 5/6章」）
- [x] マイページにコース別進捗％＋制覇バッジ、弱点復習導線
- [x] 買い切り(lifetime)を料金ページに追加＋価格設定（VIP月680/年5,980・買い切り9,800）
- [x] Supabase同期を進捗(`journey_progress`)・既読(`seen_terms`)まで拡張

### 整合・検証
- [x] narrative（卒業/最終/現在地）の位置ズレを是正（中級finale=m8、全体finale=a3）
- [x] docs/05・CLAUDE.md・docs/03 を現状に更新
- [x] devサーバー実機でデモのレンダリング確認、`tsc`・本番ビルドともにグリーン

### 2026-07-19（追撃セッション）— バックログ消化（自走）
- [x] **静的デモをインタラクティブ化（第1弾）**：`function`＝a/bを増減して `add()` 実行→戻り値表示、`props`＝label/variant を切替えてボタンが変化、`seo`＝「SEO対策する」で検索順位が入れ替わる（`LiveExample.tsx` の `ConceptFunctionDemo`/`ConceptPropsDemo`/`ConceptSeoDemo`）
- [x] **静的デモをインタラクティブ化（第2弾）**：`component`＝＋/−で部品(Button)を1〜6個に増減、`https`＝http↔httpsトグルで鍵/警告が切替、`class`＝「new で実体をつくる」でインスタンスA〜Dが増える（`ConceptComponentDemo`/`ConceptHttpsDemo`/`ConceptClassDemo`）
- [x] **静的デモをインタラクティブ化（第3弾）**：`xss`＝エスケープON/OFFで「文字として安全」↔「実行されて危険」を体感、`ci`＝「コミットして実行」でコミット→テスト→OKが順に点灯、`tailwind`＝p-4/bg-blue-600/rounded-xl をトグルして箱の見た目が変化（`ConceptXssDemo`/`ConceptCiDemo`/`ConceptTailwindDemo`）
- [x] **「この見た目のコード」を＋12用語**（`codeSnippets.ts`）：modal/toast/tab/text-field/checkbox/toggle-switch/alert/divider/rating/stat-card/skeleton-loader/segmented-control。すべて絵文字なしのコピペ可能な最小HTML/CSS。既存 `badge` の絵文字🔔もSVGベルに置換（絵文字禁止方針の徹底）
- [x] **レッスン本文（story）の図鑑リンク自動化**（`src/lib/linkify.tsx`）：誤検出を抑えるため、リンク対象を各レッスンの `relatedSlugs` に限定し、用語名（nameJa＋aliases）が本文に出た最初の1回だけ図鑑へリンク（新タブ）。`MascotTeacher.lines` を `ReactNode[]` 化して対応
- [x] **誤検出リスクの検証**：全レッスンをシミュレートし 47レッスン／85リンクを目視 → 誤ターゲット0件（本文に名前が出ない `relatedSlug` はリンクしない保守挙動）。`tsc`・本番ビルドともにグリーン
- [x] **通し復習モード** `/learn/marathon`：章末テストをコース順にまとめて連続受験。範囲（すべて/初級/中級/上級）を選択、各範囲の出題数プレビュー付き。VIPロック章は含めない（開放範囲のみ）。即時○×＋解説、誤答は弱点復習に登録・正解は弱点から消去（`recordJourneyMiss`/`resolveJourneyMiss`）、完了で `recordActivity`。`/learn` に「通し復習／弱点復習」導線カードを追加
- [x] **図鑑検索のレッスン横断**：図鑑の検索窓で用語と一緒にレッスンもヒット（`search.ts` の `matchLesson`＋`LessonIndexItem`）。journey本体はクライアントに載せず、`/zukan`（server）で正規化済みの軽量索引を作って prop で渡す設計（バンドル増 +0.3kB のみ）。検索時に「レッスンでも学べる」枠を表示し `/learn/[nodeId]` へ導線
- [x] **プロフィールを学習進捗と連動**：`/profile` のプレビューカードに Lv・称号（`level.ts` 算出＝既読/クリア/クイズ/お気に入りから）と、コース制覇バッジ（`levelProgressList` で初級/中級/上級を全クリアしたら点灯）を追加。保存なし・既存データから算出（コスト0）
- [x] **Stripe本番実装（コード土台・安全フォールバック）**：サブスク（VIP・mode=subscription）＋買い切り（lifetime・mode=payment）の両方を `/api/stripe/checkout` で発行（`plan` パラメータ＋`priceIdForPlan`/`checkoutModeForPlan`、userId/planをmetadataに）。`/api/stripe/webhook` で署名検証後、service-role（`src/lib/supabase/admin.ts`）経由で `subscriptions`/`purchases` に反映（`current_period_end` はStripe版差に強い取得）。`purchases` 表＋RLSを `schema.sql` に追加。`PlanSync`（layout常設）がログイン時に実課金状態を読んでプランへ反映（引き上げのみ）。`.env.example` に `STRIPE_LIFETIME_PRICE_ID` 追加。**キー未設定の現状は checkout→`{demo:true}` / webhook→`{skipped:true}` で安全動作（実サーバーで確認済み）。本番有効化＝実キー投入＋下記チェックリストは未実施**

---

## バックログ（次にやる候補）

優先度は目安。着手時はこの節を上から潰し、済んだら「完了リスト」へ移す。

### 高
- [ ] **Stripe 本番“有効化”**（コード土台は実装済／これは本番前チェックリスト対象・要ユーザー確認）：
  - [ ] 実キー投入（`STRIPE_SECRET_KEY` sk_test→sk_live、`STRIPE_PRICE_ID`＝サブスク価格、`STRIPE_LIFETIME_PRICE_ID`＝買い切り価格、`STRIPE_WEBHOOK_SECRET`、`SUPABASE_SERVICE_ROLE_KEY`）
  - [ ] Stripe ダッシュボードで Webhook エンドポイント `/api/stripe/webhook` を登録
  - [ ] `schema.sql`（`purchases` 追加版）を Supabase で再実行
  - [ ] テスト決済でサブスク/買い切りとも、購入→webhook→`subscriptions`/`purchases`→`PlanSync` で開放されるか通し確認
  - [ ] 解約・返金時のダウングレード（`PlanSync` は現状“引き上げのみ”。本番はサーバー側判定で失効も反映）
  - [ ] 特商法表記・課金の同意導線

### 中
- （高・中はすべて消化。次の候補は下記「低・将来」へ）

### 低・将来
- [ ] 用語の実スクショ（`term_images`）とデモの併用
- [ ] 学習データのDB完全移行（現状ローカルファースト＋ログイン同期）
- [ ] スクール向けチームプラン、アプリ(PWA)展開

---

## 運用メモ
- **章を挿入したら**：前章テストの `deepDive`（次章への橋渡し文）を更新。順序は `chapters` 配列順で決まる（id順ではない）。
- **図鑑に新語を足したら**：`terms.ts` に加え、デモを作るなら `LiveExample.tsx` の `demos` と `visualTerms.ts` の `VISUAL_SLUGS` の両方に登録。
- **有料出し分け**：本番は必ずサーバー側でも判定（現状のクライアント判定は表示制御のみ）。
