# 09. 開発進捗ログ・やることリスト（子ドキュメント）

> 親: [00_要件定義書.md](./00_要件定義書.md)
> 直近の実装セッションで何をやったか（完了リスト）と、次にやる候補（バックログ）をここに残す。
> 最終更新: 2026-07-19

---

## 現在の到達点（サマリ）

| 領域 | 状態 |
|---|---|
| 学習（すごろく `/learn`） | **30章 / 121ノード**（レッスン90・テスト31）。3レベル制（初級6・中級12・上級12）。順序は `chapters` 配列順、a3が最終章（サーバー章a12はa11とa3の間） |
| レッスンの中身 | 物語＋`takeaways`＋写経`codeSample`＋ミニ練習`practice`＋図鑑相互リンク`relatedSlugs` |
| テスト | 8割合格で解放。合格後の深掘り`deepDive`＋バッジ獲得（`awards.ts`） |
| コース目次 | `/curriculum`（全章一覧・レベル達成トロフィー・続きから再開） |
| 弱点復習 | `/learn/review`（テスト誤答だけ再挑戦・正解で消える）。記録は `cocre:journey-weak:v1` |
| 図鑑 | **298語すべてにデモ実装＝デモ100%**。詳細⇔レッスンの双方向リンク |
| 課金設計 | 無料/VIP(サブスク)/lifetime(買い切り)。中上級コンテンツがVIPの主軸（docs/05） |
| 同期 | お気に入り・成績・進捗・既読を Supabase 同期（`SupabaseSync`。テーブル未作成でも無害） |

---

## 完了リスト（実装セッション 2026-07-16〜19）

### コンテンツ拡充
- [x] 初心者コースと必修コースの用語重複を解消（roadmap廃止→レッスン末尾の図鑑導線に吸収、`/map`→`/learn`リダイレクト）
- [x] 3レベルのコース制（`level`/`access`）を導入し、中上級をVIPの主軸に
- [x] 中級を b1〜m11 まで拡充（HTML/CSS/JS/配列/データ/API実践/フォーム/状態/localStorage/Tailwind/デバッグ）
- [x] 上級を a1〜a11・a3 まで拡充（React/道具/テスト/速く/a11y/Git実践/セキュリティ/型/デザイン/SEO/AI）
- [x] 全レッスンに写経コード・ミニ練習を付与、テストに深掘りを付与
- [x] 章タイトルを「動詞で伝わる」題名に統一

### 図鑑
- [x] 中上級の新語を図鑑化（variable/function/array/props/state/tailwind/fetch/hash/xss ほか）
- [x] **デモ未実装だった27語すべてに図解デモを実装 → デモ100%達成**
- [x] `array`・`loop` をインタラクティブ化
- [x] 図鑑詳細→レッスンの逆リンク（`lessonForSlug`）

### 機能・UX
- [x] コース目次ページ `/curriculum`（レベル達成トロフィー・続きから再開）
- [x] レッスン弱点復習 `/learn/review`
- [x] `/learn` にレベルジャンプ＋弱点復習バナー、レッスン詳細に現在地表示（例「初級コース 5/6章」）
- [x] マイページにコース別進捗％＋制覇バッジ、弱点復習導線
- [x] 買い切り(lifetime)を料金ページに追加＋価格設定（VIP月680/年5,980・買い切り9,800）
- [x] Supabase同期を進捗(`journey_progress`)・既読(`seen_terms`)まで拡張

### 整合・検証
- [x] narrative（卒業/最終/現在地）の位置ズレを是正（中級finale=m8、全体finale=a3）
- [x] docs/05・CLAUDE.md・docs/03 を現状に更新
- [x] devサーバー実機でデモのレンダリング確認、`tsc`・本番ビルドともにグリーン

### 2026-07-20（キャラ演出さらに強化：コンボ途切れ・指さし・図鑑ミニキャラ）
- [x] **コンボが途切れた瞬間の演出**：3連以上のコンボ中に不正解すると、キャラの周りに**かけらが飛び散って落ちる**（`shard`、レベル色）。TestView/QuizRunnerで`brokeCombo`（途切れた連続数）を記録して`LevelMascot`へ。言葉なし
- [x] **問題を読むときの指さし**：新しい問題（idle＋nonce更新）でキャラ下に**下向きの指さし**が出てぴょこぴょこ（`point-bounce`）、下の問題文へ視線誘導
- [x] **図鑑カードにミニキャラ**（`MiniMascot.tsx`）：カーソル追従なしのCSS軽量版（298枚でも軽い・まばたきのみ）。カードホバーで上部中央に“ひょこっ”と出る。ZukanBrowserのカードに配置（`relative`＋group-hover）
- [x] keyframe `shard`/`point-bounce` 追加。`tsc`・本番ビルド337ページ グリーン。※要ユーザー目視

### 2026-07-19（キャラ演出を強化：コンボ・アイドルしぐさ・レッスン本文の表情）
- [x] **連続正解（コンボ）で特別エフェクト**（`LevelMascot`に`combo`prop）：3連続以上で広がる光の輪＋キラキラ増量、5連続でさらに二重リング＋金色。**言葉・数字は使わずエフェクトのみ**。TestView/QuizRunnerで連続正解を数え（不正解で0）配線
- [x] **キャラのアイドルしぐさ**（まばたき以外）：待機中ときどき「首かしげ」「のび」をする（`gesture-tilt`/`gesture-stretch`、3.5〜7秒間隔・タブ非アクティブ時は停止）。外=hover-bob／内=しぐさ の二層で合成
- [x] **レッスン本文でもキャラが表情を変える**（`MascotFace`に`expression`/`nonce`）：語りに合わせ 話す（口オープン）→考える（上目・への字）→ふつう→…を巡回、最後の行はにっこり（目が弧）。行送りごとに小さくうなずく（`nod`）。`MascotTeacher`が行indexから表情を出し分け
- [x] keyframe `ring-burst`/`gesture-tilt`/`gesture-stretch`/`nod` 追加。`tsc`・本番ビルド337ページ グリーン。※要ユーザー目視

### 2026-07-19（レベル別キャラの質向上：上級をロボットに・固有の動き）
- [x] **上級キャラをフクロウ→ロボットに差し替え**（Duolingoのフクロウと被る懸念に対応）。窓→動物(ネコ)→ロボの流れで独自性確保
- [x] **各キャラに固有のアイドル動作を追加して質を向上**：初級=アンテナ(ポインタ)がぴょこ動く、中級=**尻尾がゆらゆら**＋耳、上級=**アンテナ光が点滅**＋ボルト＋画面フェイス（正解=緑/不正解=赤に画面色が変化）。全体は`hover-bob`でふわふわ。正解=ネコは尻尾ピン・ロボは画面が緑、不正解=ネコは耳がへたる・ロボ画面が赤。keyframe `tail-wag`/`antenna-blip`/`hover-bob` 追加
- [x] `tsc`・本番ビルド337ページ グリーン。※要ユーザー目視

### 2026-07-19（レベル別キャラの反応演出＋ログイン演出の微修正）
- [x] **ログイン成功でも演出が出る／早すぎる問題を修正**：成功時の自動リダイレクト（`useEffect`）を`celebrating`中は止めるようにガード。これでログイン/新規登録どちらも約2秒の演出を最後まで見せてから遷移
- [x] **演出から文字を撤去**（エフェクトのみ）：祝福オーバーレイの「ようこそ」テキストを削除。以後、演出に言葉は使わない方針
- [x] **レベル別キャラ `LevelMascot.tsx` を新設し、レッスンのテスト(TestView)と問題集(QuizRunner)に配置**：初級=いつものブラウザ窓キャラ／中級=ネコ／上級=フクロウ（試験モードも上級キャラ）。カーソルで目線が動き、**正解で表情がにっこり＋周りにキラキラ（レベルごとに形・色ちがい：初級=稲妻緑/中級=ハート桃/上級=トロフィー金）、不正解で しょんぼり顔＋がっかり演出（初級=汗/中級=いかりマーク/上級=グレーのもや）**。keyframe `shake`/`hop`/`spark`/`sweat` を globals.css に追加
- [x] `tsc`・本番ビルド337ページ グリーン。※キャラ演出はスクショ未確認＝要ユーザー目視

### 2026-07-19（ログインのキャラを「ビル群」化＋成功時の祝福演出）
- [x] **ログイン左のキャラを箱型・円柱型5体のビル群に**（`AuthCity.tsx`、旧`AuthMascot`は削除）：高さバラバラのスカイライン。カーソルで各ビルが視差でゆれ（`usePointerParallax`）、目線もカーソル追従（`useEyeTracking`共有offset）。パスワード入力中は目を閉じる（`peeking`）。窓・アンテナでビルらしさ。動きの仕様は据え置き（ユーザー「動きはこれでいい」）
- [x] **登録・ログイン成功時に約2秒の「喜ぶエフェクト」→画面遷移**（`login/page.tsx`＋`Confetti.tsx`）：成功で`celebrating`→全画面オーバーレイ（紙吹雪＋ビル群が`celebrate-hop`で飛び跳ねる＋「ようこそ、Co-Creへ！」）を2秒表示してから`router.replace("/mypage")`。keyframe `confetti-fall`/`celebrate-hop` を globals.css に追加
- [x] `tsc`・本番ビルド337ページ グリーン。※キャラ演出はスクショ未確認＝要ユーザー目視

### 2026-07-19（ナビ改善＋ログイン画面のキャラ演出）
- [x] **ナビのショートカットを「クリックで遷移＋ホバー/シェブロンで一覧」に**（`MainNav.tsx`）：submenu付き項目（図鑑/レッスン/問題集/マイページ）は、本体クリックでその項目のメインページへ遷移し、ホバーか右のシェブロンボタンで一覧を開く（以前は本体クリックが遷移せず不評だった）。**レッスンのsubmenuを復活**（目次/初級・中級・上級コース=/learn/course/[level]/全章目次/道具ガイド）
- [x] **ログイン/新規登録を2カラム化＋相棒キャラ演出**（`login/page.tsx`＋`AuthMascot.tsx`）：右にフォーム、左に**カーソルで目線＋体がゆれるキャラ**（`useEyeTracking`＋`usePointerParallax`）。**パスワード欄フォーカス中は目を閉じ、手で目をかくす演出**（`Eye`に`closed`追加、`peeking` state を password/confirm の onFocus/onBlur で切替）。ログイン=緑・新規登録=紫でパネル色も切替。モバイルはキャラ→フォームの縦積み
- [x] 本番ビルド337ページ・`tsc`グリーン。※ヒーロー同様この環境ではスクショ未確認＝要ユーザー目視

### 2026-07-19（レッスンUX大改修＋飛び級＋ヒーロー＋友達進捗モック）
- [x] **友達招待の特典を VIP7日→3日**（`invite.ts` の `INVITE_REWARD_DAYS`、AccountMenu表記も）
- [x] **ヒーローの浮遊アイコンをカーソル視差追従**（`mascotEyes.tsx` に `usePointerParallax`、`HeroMascot` の標本に depth 視差／外=視差・内=浮遊で分離）。**画面外・タブ非アクティブ時は追従停止**（`useEyeTracking` にもIntersectionObserver＋visibility停止を追加）＝負荷対策
- [x] **レッスンを「目次ハブ＋レベル別ページ」に再構成**（長い1画面スクロールを解消）：`/learn`＝ハブ（続きから直行CTA＋初級/中級/上級の入口カード＝`LevelHub.tsx`）、`/learn/course/[level]`＝そのコースの道だけ表示（新規）。`JourneyBoard` に `level?` prop を追加してレベル絞り込み対応、**上部に「つづきから」「現在地へ（該当マスへスクロール）」ボタン**、各マスに `id=node-<id>`＋`scroll-mt-24`。ナビ「レッスン」は**ホバー式サブメニューをやめて通常リンクに戻した**（不評だったため）。目次/道具ガイド導線は/learn下部へ移設
- [x] **飛び級（スキップ）テスト**（`/learn/skip/[level]`＋`skipTest.ts`）：そのレベルの章末テストから横断出題（10問・**合格9割**＝通常8割より少し難）。合格でそのレベルの全マスを `markNodeCleared` で一括クリア扱い→次コースへ導線。各コースページ＆VIPロック表示から入口。UIはDuolingo式の即時○×
- [x] **友達の進捗（モック）**（`FriendsProgress.tsx`＋`friendsMock.ts`）：招待ページに、ダミー友達＋あなたを進捗順にランキング表示（今どのコース/何マスか＝`progressLabel`）。※本番はSupabase必要（コストは会話でユーザーに説明）
- [x] `tsc`・本番ビルド（337ページ）グリーン。新ルート `/learn/course/[level]`・`/learn/skip/[level]` 生成確認

### 2026-07-19（自走：新章追加＋孤立語の学習動線づけ）
- [x] **上級に新章「裏側を動かす（サーバーとデータベース）」a12 を追加**（`journey.ts`）：a11(SEO)とa3(AI最終章)の間に挿入し、a3を最終章のまま維持。3レッスン（l1=サーバー/リクエスト・レスポンス/エンドポイント/環境、l2=データベース/SQL/ORM、l3=認証/認可/JWT/セッション/APIキー/CORS/Webhook）＋テスト。写経コード(fetch/SQL)・ミニ練習つき。**図鑑のbackendカテゴリ14語すべてを relatedSlugs で学習動線に接続**（これまでレッスン章が無く全て孤立していた）。a11-testのdeepDiveを次章a12への橋渡しに更新。`awards.ts` に章クリア賞「裏側もわかる」(server-dev)追加＋上級制覇リストに a12 を追加。→ **章 29→30・マス 117→121**（自動集計。/learn・/curriculum・レベル進捗に自動反映）
- [x] **孤立語→レッスンの学習動線づけ（6レッスン）**：relatedSlugs未設定だったレッスンに既存の実物デモ語を接続。c5-l3=color-code/color-swatch/accessibility、m2-l2=color-code/rem-em、m4-l2=json、m6-l2=list-group/empty-state、a5-l3=lazy-loading/skeleton-loader/loading-bar、a8-l1=xss/https。全slug実在を検証済み
- [x] `tsc`・本番ビルドともにグリーン（全298語詳細＋新章マス生成）

### 2026-07-19（ナビ/目次/メンテ設計セッション）
- [x] **ナビバーの「レッスン」にレベルショートカット追加**（`MainNav.tsx`）：ホバー/タップで 学習の道のり・初級/中級/上級コース・コース目次 を開ける（図鑑/問題集と同じsubmenu方式）。各レベルは `/curriculum#beginner|intermediate|advanced` へ
- [x] **コース目次にレベルジャンプ導線**（`curriculum/page.tsx`）：各levelセクションに `id`＋`scroll-mt-24` を付与し、ヘッダー下に進捗％つきのジャンプバー（初級→中級→上級）を追加
- [x] **時点依存の要メンテ箇所を集約**（`docs/11_時点依存の要メンテ箇所.md` 新規）：AI章はツール名を書かない方針＝陳腐化回避（🧊）、価格の散在箇所（vip/PRICE・特商法・docs/05）、キー投入場所などを一覧化。CLAUDE.mdのドキュメント索引にも追加
- [x] **道具とAIガイド `/tools` を新規実装**（`src/app/tools/page.tsx`＋`src/data/tools.ts`）：エディタ(VS Code/Cursor)・対話AI(ChatGPT/Claude/Gemini)・補完/エージェント(Copilot/Claude Code)を「無料/日本語/得意」の軸つきで具体名で掲載。「まずこの2つ」導線、`TOOLS_LAST_UPDATED`表示、景表法対応の広告表記、下部にアフィリエイト/広告枠(`AdSlot`)。各ツールに `affiliateUrl` を入れると rel=sponsored のアフィリンクに切替。レッスンnavのsubmenuからも到達可。docs/11に時点依存として登録
- [x] `tsc` グリーン

### 2026-07-19（設定サポートセッション）
- [x] **特定商取引法に基づく表記ページを実装**（`src/app/legal/tokusho/page.tsx`）：販売価格（VIP月680/年5,980・買切9,800＝`vip/page.tsx` の `PRICE` と一致）・支払方法(Stripe)・提供時期・解約/返品・動作環境などを既存法令ページと同じ体裁で記載。氏名/住所/電話など個人情報は琥珀色のプレースホルダ（オーナーが公開前に確定）。マイページ「その他」に導線追加。`tsc`グリーン。docs/10-D の1項目に対応
- [x] **PWA状態を確認**：manifest(`src/app/manifest.ts`)・`public/sw.js`・`PwaRegister`・`appleWebApp`設定・アイコン(`logo_app.png`)まで既に完備済みと確認（追加実装不要）

### 2026-07-19（追撃セッション）— バックログ消化（自走）
- [x] **静的デモをインタラクティブ化（第1弾）**：`function`＝a/bを増減して `add()` 実行→戻り値表示、`props`＝label/variant を切替えてボタンが変化、`seo`＝「SEO対策する」で検索順位が入れ替わる（`LiveExample.tsx` の `ConceptFunctionDemo`/`ConceptPropsDemo`/`ConceptSeoDemo`）
- [x] **静的デモをインタラクティブ化（第2弾）**：`component`＝＋/−で部品(Button)を1〜6個に増減、`https`＝http↔httpsトグルで鍵/警告が切替、`class`＝「new で実体をつくる」でインスタンスA〜Dが増える（`ConceptComponentDemo`/`ConceptHttpsDemo`/`ConceptClassDemo`）
- [x] **静的デモをインタラクティブ化（第3弾）**：`xss`＝エスケープON/OFFで「文字として安全」↔「実行されて危険」を体感、`ci`＝「コミットして実行」でコミット→テスト→OKが順に点灯、`tailwind`＝p-4/bg-blue-600/rounded-xl をトグルして箱の見た目が変化（`ConceptXssDemo`/`ConceptCiDemo`/`ConceptTailwindDemo`）
- [x] **「この見た目のコード」を＋36用語**（`codeSnippets.ts`、17→53語）：第1弾 modal/toast/tab/text-field/checkbox/toggle-switch/alert/divider/rating/stat-card/skeleton-loader/segmented-control、第2弾 select/radio-button/range-slider/banner/price-tag/kbd/callout/back-to-top/close-button/list-group/link-text/heading、第3弾 empty-state/notification/snackbar/split-button/like-button/file-upload/char-counter/required-mark/error-message/status-dot/thumbnail/toolbar。すべて絵文字なしのコピペ可能な最小HTML/CSS。既存 `badge` の絵文字🔔もSVGベルに置換（絵文字禁止方針の徹底）
- [x] **レッスン本文（story）の図鑑リンク自動化**（`src/lib/linkify.tsx`）：誤検出を抑えるため、リンク対象を各レッスンの `relatedSlugs` に限定し、用語名（nameJa＋aliases）が本文に出た最初の1回だけ図鑑へリンク（新タブ）。`MascotTeacher.lines` を `ReactNode[]` 化して対応
- [x] **誤検出リスクの検証**：全レッスンをシミュレートし 47レッスン／85リンクを目視 → 誤ターゲット0件（本文に名前が出ない `relatedSlug` はリンクしない保守挙動）。`tsc`・本番ビルドともにグリーン
- [x] **通し復習モード** `/learn/marathon`：章末テストをコース順にまとめて連続受験。範囲（すべて/初級/中級/上級）を選択、各範囲の出題数プレビュー付き。VIPロック章は含めない（開放範囲のみ）。即時○×＋解説、誤答は弱点復習に登録・正解は弱点から消去（`recordJourneyMiss`/`resolveJourneyMiss`）、完了で `recordActivity`。`/learn` に「通し復習／弱点復習」導線カードを追加
- [x] **図鑑検索のレッスン横断**：図鑑の検索窓で用語と一緒にレッスンもヒット（`search.ts` の `matchLesson`＋`LessonIndexItem`）。journey本体はクライアントに載せず、`/zukan`（server）で正規化済みの軽量索引を作って prop で渡す設計（バンドル増 +0.3kB のみ）。検索時に「レッスンでも学べる」枠を表示し `/learn/[nodeId]` へ導線
- [x] **プロフィールを学習進捗と連動**：`/profile` のプレビューカードに Lv・称号（`level.ts` 算出＝既読/クリア/クイズ/お気に入りから）と、コース制覇バッジ（`levelProgressList` で初級/中級/上級を全クリアしたら点灯）を追加。保存なし・既存データから算出（コスト0）
- [x] **Stripe本番実装（コード土台・安全フォールバック）**：サブスク（VIP・mode=subscription）＋買い切り（lifetime・mode=payment）の両方を `/api/stripe/checkout` で発行（`plan` パラメータ＋`priceIdForPlan`/`checkoutModeForPlan`、userId/planをmetadataに）。`/api/stripe/webhook` で署名検証後、service-role（`src/lib/supabase/admin.ts`）経由で `subscriptions`/`purchases` に反映（`current_period_end` はStripe版差に強い取得）。`purchases` 表＋RLSを `schema.sql` に追加。`PlanSync`（layout常設）がログイン時に実課金状態を読んでプランへ反映（引き上げのみ）。`.env.example` に `STRIPE_LIFETIME_PRICE_ID` 追加。**キー未設定の現状は checkout→`{demo:true}` / webhook→`{skipped:true}` で安全動作（実サーバーで確認済み）。本番有効化＝実キー投入＋下記チェックリストは未実施**

---

## バックログ（次にやる候補）

優先度は目安。着手時はこの節を上から潰し、済んだら「完了リスト」へ移す。

### 高
- [ ] **Stripe 本番“有効化”**（コード土台は実装済／これは本番前チェックリスト対象・要ユーザー確認）：
  - [ ] 実キー投入（`STRIPE_SECRET_KEY` sk_test→sk_live、`STRIPE_PRICE_ID`＝サブスク価格、`STRIPE_LIFETIME_PRICE_ID`＝買い切り価格、`STRIPE_WEBHOOK_SECRET`、`SUPABASE_SERVICE_ROLE_KEY`）
  - [ ] Stripe ダッシュボードで Webhook エンドポイント `/api/stripe/webhook` を登録
  - [ ] `schema.sql`（`purchases` 追加版）を Supabase で再実行
  - [ ] テスト決済でサブスク/買い切りとも、購入→webhook→`subscriptions`/`purchases`→`PlanSync` で開放されるか通し確認
  - [ ] 解約・返金時のダウングレード（`PlanSync` は現状“引き上げのみ”。本番はサーバー側判定で失効も反映）
  - [ ] 特商法表記・課金の同意導線

### 中
- （高・中はすべて消化。次の候補は下記「低・将来」へ）

### 低・将来
- [ ] 用語の実スクショ（`term_images`）とデモの併用
- [ ] 学習データのDB完全移行（現状ローカルファースト＋ログイン同期）
- [ ] スクール向けチームプラン、アプリ(PWA)展開

---

## 運用メモ
- **章を挿入したら**：前章テストの `deepDive`（次章への橋渡し文）を更新。順序は `chapters` 配列順で決まる（id順ではない）。
- **図鑑に新語を足したら**：`terms.ts` に加え、デモを作るなら `LiveExample.tsx` の `demos` と `visualTerms.ts` の `VISUAL_SLUGS` の両方に登録。
- **有料出し分け**：本番は必ずサーバー側でも判定（現状のクライアント判定は表示制御のみ）。
