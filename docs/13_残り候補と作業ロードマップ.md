# 13. 残り候補と作業ロードマップ（トークン切れ時の引き継ぎ用）

このMDは「次にやること」をまとめた作業メモ。**内容を消化したら各項目を消して**いってOK。
すべて**無料・追加コストなし**で実装できるものだけを並べている（学生ユーザー方針）。

最終更新: 2026-07-20

---

## ✅ 完了（このセッション）

### Windowsコマンド図鑑を新規追加
- `src/data/winCommands.ts` … コマンド26件のデータ（CMD / PowerShell 両対応）
  - カテゴリ: ファイル操作 / フォルダ移動 / ネットワーク / システム / 開発でよく使う / PowerShell
  - 各コマンドに 一言・説明・具体例・別名 を収録
- `src/app/commands/page.tsx` … 検索＋カテゴリ絞り込みつきの専用ページ（`/commands`）
- 導線: `/zukan` ページ内カード ＋ マイページ「その他」にリンク追加
- ビルド確認済み（`/commands` 8.82kB, 静的生成）

**拡張するには**: `winCommands` 配列に追記するだけ。ページ側は自動で反映される。
`git`系・`ssh`・`cd /d`（ドライブ跨ぎ）・`findstr`・`start`・`explorer .` などを足すと良い。

### 効果音（Web Audio・素材ゼロ・無料）
- `src/lib/sfx.ts` … `AudioContext` を lazy 生成。`playCorrect()`（明るい2音）/
  `playWrong()`（低い2音）/ `isSfxOn()` / `setSfxOn()` / `toggleSfx()`。localStorage `sfx-on`、**デフォルトOFF**。
- `src/components/SfxToggle.tsx` … マイページ「その他」のON/OFFトグル。ONにした瞬間サンプル再生。
- 鳴らす場所（実装済み）: `TestView.tsx` / `QuizRunner.tsx` / `skip/[level]/page.tsx` / `marathon/page.tsx` の回答時。
- 方針順守: 音のみ（言葉・ボイス読み上げは無し）。

### 図鑑の孤立用語をレッスンへ接続（完了）
- `VISUAL_SLUGS`（298語）のうちレッスン未接続だった **215語 → 0語** に。
- `journey.ts` の各レッスン `relatedSlugs` にテーマ別で追記。デッドリンク0件を確認。
- 検証コマンド（孤立数を出す）:
  ```
  node -e 'const fs=require("fs");const v=fs.readFileSync("src/data/visualTerms.ts","utf8");const vs=[...v.matchAll(/"([a-z0-9-]+)"/g)].map(m=>m[1]);const j=fs.readFileSync("src/data/journey.ts","utf8");const r=new Set();for(const m of j.matchAll(/relatedSlugs:\s*\[([^\]]*)\]/g))for(const s of m[1].matchAll(/"([^"]+)"/g))r.add(s[1]);console.log(vs.filter(s=>!r.has(s)).length)'
  ```

### コマンド図鑑の充実（完了）
- Git カテゴリを新設し `git clone/add/commit/push/pull/branch/log/diff` を追加。
- `cd /d`・`tree`・`start`・`findstr`・`set/$env:`・`npm install`・`node -v` も追加。
- さらに `git init/remote/stash/restore/merge`・`explorer .`・`chcp 65001`・`ssh` を追加。
- 現在 **49コマンド**。まだ足すなら `git rebase`・`robocopy`・`xcopy`・`git config` など。
- 実機（`npm run dev` → `/commands`）でレンダリング確認済み（検索・カテゴリ・ダーク例示・別名チップ 良好）。

---

## 🔜 残りの候補（未着手・優先度順）

### 次にやると良いこと
- コマンド図鑑をさらに拡充（上記の未収録コマンド）。`winCommands` に追記するだけ。
- 図鑑用語の相互リンク（terms.ts の `related`）の見直し。
- Mac/Linux対応の需要が出たら `shell` に "bash" を足す設計に拡張可能。

---

## 🔒 本番系（ユーザーのキー・操作が必要 → AI単独では不可）
- Stripe 有効化（本番キー・商品登録・Webhook） … `docs` の該当ファイル参照
- Supabase 本番同期（friendsのモック→実データ、schema実行）
- 独自ドメイン取得・設定
- 図鑑用語の実スクショ差し込み

→ これらは着手時にユーザーへ依頼＆伴走する。勝手に進めない。

---

## メモ: 動作確認の方法（このリポジトリ）
- 型: `cd /f/code-zukan && ./node_modules/.bin/tsc --noEmit`
- ビルド: `npm run build`（339+ページ）
- 見た目確認: `npm run dev` → ブラウザ。CDPスクショは scratchpad の `shot.mjs` 参照。
